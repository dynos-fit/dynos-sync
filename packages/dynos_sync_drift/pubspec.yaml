name: dynos_sync_drift
description: Drift (SQLite) adapters for dynos_sync — LocalStore, QueueStore, and TimestampStore implementations.
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  dynos_sync:
    path: ../dynos_sync
  drift: ^2.14.0

dev_dependencies:
  drift_dev: ^2.14.0
  build_runner: ^2.4.0
