name: dynos_sync_supabase
description: Supabase adapter for dynos_sync — RemoteStore implementation using Supabase Postgrest.
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  dynos_sync:
    path: ../dynos_sync
  supabase: ^2.0.0
